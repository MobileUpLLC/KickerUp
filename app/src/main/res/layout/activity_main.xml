<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.main.MainActivity">

    <ImageView
        android:id="@+id/rhombus"
        android:layout_width="600dp"
        android:layout_height="600dp"
        android:layout_gravity="center"
        android:padding="100dp"
        android:src="@drawable/rhombus"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="ru.mobileup.kickerup.ui.main.BottomBarBehavior"/>

    <android.support.design.bottomappbar.BottomAppBar
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_app_bar_height"
        android:layout_gravity="bottom"
        android:elevation="6dp"
        android:visibility="gone"
        app:backgroundTint="@color/black"
        app:fabAlignmentMode="center"
        app:fabAttached="true"
        app:fabCradleMargin="5dp">

        <!--
        TODO: Это чудо света надо будет убрать, когда гугл допилит библиотеку
        Короче, это костыль - да.
        Но в BottomAppBar нельзя сейчас
        сделать кнопку навигации по центру
        -->
        <android.support.v7.widget.Toolbar
            android:id="@+id/bottomToolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <android.support.design.widget.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:tabIconTint="@color/white"
            app:tabIndicatorColor="@color/accent"/>
    </android.support.design.bottomappbar.BottomAppBar>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="10dp"
        android:src="@drawable/ic_arrow_forward"
        android:visibility="gone"
        app:backgroundTint="@color/accent"
        app:layout_anchor="@id/bottomBar"/>

</android.support.design.widget.CoordinatorLayout>